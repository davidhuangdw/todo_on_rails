.comment.well{id:comment.html_id}
  %p
    = link_to comment.user.username, comment.user
    = comment.created_at
    %span.pull-right= link_to 'Delete', comment, method: :delete, remote: true
  %p= best_in_place comment, :content


